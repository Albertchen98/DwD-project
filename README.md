
  # Ohne Namen

  This is a code bundle for Ohne Namen. The original project is available at https://www.figma.com/design/kqyNjBm6svZKPzR8EPcIKY/Ohne-Namen.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  